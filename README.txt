							
							       technologies used

1)concerning the android side :

-XML
-java
-kotlin

2)Concerning the database :

- using xampp as a local host
- creating the database on php myadmin using MYSQL

3)connecting the database with the android

-we used php making for each logic in the app a function with query to manipulate and maintain the database
-in the php file "includes" you can find a script called DBoperation which have a class called operations which have functions with queries  to maintain and manipulate the database,another script called register.php have functions which can be accessed from android by the URL.
      

  

 